version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - errorlint
    - gocritic
    - misspell
  settings:
    misspell:
      locale: UK
      ignore-rules:
        - Color
  exclusions:
    presets:
      - comments
